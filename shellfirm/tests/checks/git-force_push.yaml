- test: "git push -f"
  description: "match force push command"
  expected_detection_ids:
    - git:force_push
- test: "git push --force"
  description: "match force push with long option"
  expected_detection_ids:
    - git:force_push
- test: "git push --force-with-lease"
  description: "match force push with lease"
  expected_detection_ids:
    - git:force_push
